export const isValidUsername = (username) => username.length >= 3
export const isUsernameEmpty = (username) => username.trim() ===''
export const isEmailEmpty =(email) => email.trim() ===''
export const isPasswordEmpty =(password) => password.trim() ===''
export const isValidPassword = (password) => password.length >=8
